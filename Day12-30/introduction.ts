// Day 12: 介面宣告 X 使用介面 - TypeScript Interface Intro.

export {};

// 這階段主要會從 Interface 出發：
// * 介面宣告 (Interface Declaration)
// * Interface v.s. Type System

// 然後用新的觀念思考複合型別：
// * union 以及 intersection

// 接下來會介紹 TypeScript 的類別 (Class)：
// * 建構子 (Constructor)
// * 類別屬性與方法 (Member Variables(Properties) & Methods)
// * 存取權限修飾子 (Access Modifiers)
// * 存取值方法 (Accessors)

// Class 進階觀念：
// * 繼承 (Inheritance)
// * 靜態屬性與方法 (Static)
// * 私有建構子與單例模式 (Private Constructor & Singleton Pattern)
// * 抽象類別 (Abstract Class)

// 然後做一個統整：
// Class + Inheritance v.s. Class + TS Interface
// TS Class V.S. ES6 Class

// 最後要討論一句非常有名的話：
// 號稱 JS 圈被誤解得最嚴重的一句話其中之一
// "Favor object composition over class inheritance."
